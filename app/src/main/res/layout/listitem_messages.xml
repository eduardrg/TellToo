<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="16dp">

    <ImageView
        android:id="@+id/messages_to_image"
        android:layout_gravity="center"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/place_holder_img"/>

    <ImageView
        android:id="@+id/messages_type"
        android:layout_gravity="center"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/place_holder_img"
        android:layout_toRightOf="@id/messages_to_image"/>

    <ImageView
        android:id="@+id/messages_from_image"
        android:layout_gravity="center"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/place_holder_img"
        android:layout_toRightOf="@id/messages_type"/>

    <TextView
        android:id="@+id/message_test"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SicklyCheckeredLemur continued your story."
        android:gravity="center"
        android:textAlignment="center"
        android:layout_toRightOf="@id/messages_from_image"
        android:layout_alignTop="@id/messages_from_image"
        android:layout_alignBottom="@id/messages_from_image"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"/>

    <View
        android:layout_width="wrap_content"
        android:layout_height="1dp"
        android:background="@drawable/place_holder_img"
        android:layout_marginTop="16dp"
        android:layout_below="@id/messages_from_image"/>

</RelativeLayout>